version: '3'

services:
  app:
    container_name: order_summary_component
    build: .
    labels:
      - "traefik.enable=true" # <== Enable traefik to proxy this container
      - "traefik.http.routers.summary.rule=Host(`summary.mytestingenlocal.ml`)" # <== Your Domain Name for the https rule
      - "traefik.http.routers.summary.entrypoints=web-secured" # <== Defining entrypoint for https, **ref: line 31
      - "traefik.http.services.summary.loadbalancer.server.port=3000" # <== port running
      - "traefik.http.routers.nginx-secured.tls.certresolver=mytlschallenge" # <== Defining certsresolvers for https
    networks:
      - web
    env_file:
      - .env

networks:
  web: